#include <graphics.h>
#include <conio.h>

int main() {
    int gd = DETECT, gm;
    initgraph(&gd, &gm, "");

    ellipse(320, 240, 0, 360, 100, 50);  // Draws an ellipse with center (320, 240), x radius 100, and y radius 50

    getch();
    closegraph();
    return 0;
}
