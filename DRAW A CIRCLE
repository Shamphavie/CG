#include <graphics.h>
#include <conio.h>

int main() {
    int gd = DETECT, gm;
    initgraph(&gd, &gm, "");

    circle(300, 200, 50);  // Draws a circle at (300, 200) with radius 50

    getch();
    closegraph();
    return 0;
}
